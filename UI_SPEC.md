# 健康ナビ — UI 仕様書

> 作成日: 2026-04-30  
> フレームワーク: React 18 + Vite / React Router v6

---

## 目次

1. [全体構成・ルーティング](#1-全体構成ルーティング)
2. [デザインシステム](#2-デザインシステム)
3. [レイアウト](#3-レイアウト)
4. [ダッシュボード](#4-ダッシュボード)
5. [マイページ](#5-マイページ)
   - [設定タブ](#51-設定タブ)
   - [グループタブ](#52-グループタブ)
6. [プランページ](#6-プランページ)
7. [買い物リスト](#7-買い物リスト)
8. [食事ログ](#8-食事ログ)
9. [認証画面](#9-認証画面)
10. [チャットウィジェット](#10-チャットウィジェット)
11. [既知の課題・TODO](#11-既知の課題todo)

---

## 1. 全体構成・ルーティング

```
/            → DashboardPage    (ログイン必須)
/plan        → MealPlanPage     (ログイン必須)
/list        → ShoppingListPage (ログイン必須)
/me          → MyPage           (ログイン必須)
/meals       → MealLogPage      (ログイン必須、旧ページ・直リンク用)
/body        → /me へリダイレクト
/group       → /me へリダイレクト
/settings    → /me へリダイレクト
/meal-plan   → /plan へリダイレクト
/login       → LoginPage        (未ログイン専用)
/register    → RegisterPage     (未ログイン専用)
```

- 未認証アクセス → `/login` へリダイレクト
- 認証済みで `/login` アクセス → `/` へリダイレクト

---

## 2. デザインシステム

### カラーパレット

| トークン | 値 | 用途 |
|---|---|---|
| `--brand` | `#16a34a` | メインブランドカラー（グリーン）|
| `--brand-hover` | `#15803d` | ホバー時 |
| `--brand-pale` | `#f0fdf4` | 薄い緑背景 |
| `--brand-light` | `#bbf7d0` | ボーダー・アクセント |
| `--brand-text` | `#15803d` | テキスト上のブランドカラー |
| `--text` | `#0f172a` | 本文 |
| `--text-2` | `#64748b` | サブテキスト |
| `--text-3` | `#94a3b8` | プレースホルダー・軸ラベル |
| `--surface` | `#ffffff` | カード・パネル背景 |
| `--bg` | `#f8fafc` | ページ背景 |
| `--border` | `#e2e8f0` | ボーダー |
| `--red` | `#dc2626` | エラー・超過 |
| `--orange` | `#ea580c` | 警告 |
| `--blue` | `#2563eb` | 情報・睡眠グラフ |

### スペーシング

```
--sp-1: 4px  --sp-2: 8px  --sp-3: 12px  --sp-4: 16px
--sp-5: 20px --sp-6: 24px --sp-8: 32px  --sp-10: 40px
```

### 角丸

```
--r-xs: 6px  --r-sm: 8px   --r-md: 12px  --r-lg: 16px
--r-xl: 20px --r-2xl: 24px --r-pill: 999px
```

### タイポグラフィ

- フォント: `-apple-system, BlinkMacSystemFont, 'Hiragino Sans', 'Noto Sans JP'`
- ベースサイズ: `14px`
- ページタイトル: `24px / 800 weight`
- ウィジェット値: `28px / 800 weight`
- セクションタイトル: `11px / 700 weight / uppercase / letter-spacing 0.08em`

### コンテナ最大幅（PC 最適化方針）

**設計原則**: モバイルファーストだが、PC で間延びしないよう**コンテンツに最大幅キャップ**を設定する。Twitter/X や GitHub と同様の発想で、行長を抑えて可読性を保つ。

| トークン | 値 | 用途 |
|---|---|---|
| `--container-tablet` | `768px`  | Tablet L 以上のページ最大幅 |
| `--container-desktop` | `1024px` | Desktop のページ最大幅 |
| `--container-wide` | `1280px` | Wide Desktop (≥1440px) のページ最大幅 |
| `--container-narrow` | `560px`  | フォーム・モーダル等の絞り込みコンテンツ |
| `--container-reading` | `680px`  | 文章主体のページ（規約等） |

```css
:root {
  --container-tablet:  768px;
  --container-desktop: 1024px;
  --container-wide:    1280px;
  --container-narrow:  560px;
  --container-reading: 680px;
}
```

これらは `app-main` および各種コンテンツコンテナの `max-width` として参照される。

### ボタン種別

| クラス | 見た目 | 用途 |
|---|---|---|
| `.btn-primary` | 緑べた塗り・白文字 | 主要アクション |
| `.btn-secondary` | 薄緑背景・緑文字 | 副次アクション |
| `.btn-outline` | 白背景・グレーボーダー | 補助アクション |
| `.btn-ghost` | 透明背景 | ナビ・軽量操作 |
| `.btn-danger` | 赤べた塗り・白文字 | 削除・解除 |
| `.btn-sm` | `min-height: 36px` | コンパクト |
| `.btn-lg` | `min-height: 52px` | 大きめ |
| `.btn-full` | `width: 100%` | 全幅 |

---

## 3. レイアウト

### 3.1 ブレイクポイント設計

| 名称 | 範囲 | 主な変化 |
|---|---|---|
| **Mobile**       | `< 480px`         | ウィジェット 1 列、ボトムナビ表示 |
| **Tablet S**     | `480px – 767px`   | ウィジェット 2 列、ボトムナビ表示 |
| **Tablet L**     | `768px – 1023px`  | ボトムナビ非表示、サイドバーまだ非表示（ヘッダーバー想定） |
| **Desktop**      | `1024px – 1439px` | サイドバー表示（216px）、コンテンツ最大 1024px |
| **Wide Desktop** | `≥ 1440px`        | コンテンツ最大 1280px、ウィジェット 3 列でゆとり |

### 3.2 シェル構造

```
Desktop / Wide Desktop (≥ 1024px):
┌───────────────────────────────────────────────────────────────┐
│  [side-nav]   │   [app-body]                                  │
│  216px 固定   │   ┌─────────────────────────────────────────┐ │
│               │   │  app-main (max-width 1024–1280px)       │ │
│               │   │  ┌────────────────────────────────────┐ │ │
│               │   │  │ <ページコンテンツ>                 │ │ │
│               │   │  └────────────────────────────────────┘ │ │
│               │   │  <ChatWidget /> (右下固定)              │ │
│               │   └─────────────────────────────────────────┘ │
└───────────────────────────────────────────────────────────────┘

Mobile / Tablet (< 1024px):
┌──────────────────────────────────────────┐
│  app-main (full-width, padding 16px)     │
│  <ページコンテンツ>                      │
│  <ChatWidget />                          │
├──────────────────────────────────────────┤
│  [bottom-nav] — 固定フッター 60px        │  ← <768px のみ
└──────────────────────────────────────────┘
```

### 3.3 ナビゲーション項目

| ルート | ラベル | アイコン |
|---|---|---|
| `/` | ダッシュボード | `LayoutDashboard` |
| `/plan` | プラン | `CalendarDays` |
| `/list` | リスト | `ListChecks` |
| `/me` | マイページ | `UserCircle2` |

- **デスクトップ**: 左サイドバー（`side-nav` / 幅 216px）、アクティブ時に `brand-pale` 背景
- **モバイル / タブレット**: 下部固定ナビ（`bottom-nav` / 高さ 60px）、アクティブ時にアイコン背景が `brand-pale` + `translateY(-1px)`
- **ブランドロゴ**: `Leaf` アイコン + 「健康ナビ」テキスト（サイドバーのみ）

### 3.4 `app-main` の幅・パディング設計

PC で内容が横に間延びしないよう、コンテンツ領域には**段階的な最大幅**を設定する。

| ブレイクポイント | `padding` | `max-width` | `margin` |
|---|---|---|---|
| Mobile (`< 480px`)        | `16px` (下端 `72px + safe-area`) | なし（全幅）    | — |
| Tablet S (`≥ 480px`)      | `20px` (下端 `72px + safe-area`) | なし（全幅）    | — |
| Tablet L (`≥ 768px`)      | `24px` (下端 `32px`)            | `768px`        | `0 auto` |
| Desktop (`≥ 1024px`)      | `32px`                          | `1024px`       | `0 auto` |
| Wide Desktop (`≥ 1440px`) | `40px`                          | `1280px`       | `0 auto` |

```css
.app-main {
  flex: 1;
  overflow-y: auto;
  padding: 16px;
  padding-bottom: calc(72px + env(safe-area-inset-bottom, 8px));
}

@media (min-width: 480px)  { .app-main { padding: 20px; } }
@media (min-width: 768px)  { .app-main { padding: 24px; padding-bottom: 32px; max-width: 768px;  margin: 0 auto; } }
@media (min-width: 1024px) { .app-main { padding: 32px;                       max-width: 1024px; } }
@media (min-width: 1440px) { .app-main { padding: 40px;                       max-width: 1280px; } }
```

> **設計意図**: 1280px を上限にすることで、4K モニターなどで間延びせず可読性を保つ。サイドバー(216px) + コンテンツ(1280px) + 余白で約 1500px の有効領域に収まる。

### 3.5 カード・要素の横幅ルール

| 要素 | 推奨幅 |
|---|---|
| `.card` | 親要素の100%（コンテナ最大幅で制限） |
| `.card` 内の `padding` | Mobile: `16px` / Tablet以上: `20px` / Desktop: `24px` |
| ページタイトル領域 (`.page-header`) | 100%（折り返しなし） |
| フォーム入力 (`.form-input`) | デフォルト 100%、明示的に `max-width: 240–360px` を指定して伸びすぎ防止 |
| モーダル (`.modal-sheet`) | Mobile: 全幅ボトムシート / Tablet以上: `max-width: 560px` でセンタリング |
| アラート (`.alert`) | 100% |

---

## 4. ダッシュボード

### 4.1 概要

日付ごとの健康データを**ウィジェット**形式で表示。ウィジェットは6種類あり、表示/非表示・並び順をカスタマイズ可能。

**Desktop (≥ 1024px) 想定レイアウト:**

```
┌─────────────────────────────────────────────────────────────────┐
│  ダッシュボード                              [設定] [同期]      │
├─────────────────────────────────────────────────────────────────┤
│ (ウィジェット設定パネル — 展開時のみ)                           │
├─────────────────────────────────────────────────────────────────┤
│              ← [今日 · 4月30日(水)] →                            │
├─────────────────────────────────────────────────────────────────┤
│ [未連携バナー — 未連携時のみ]                                   │
├──────────────────┬──────────────────┬───────────────────────────┤
│  カロリー (1d)   │   PFC (1d)       │   歩数 (1d)               │  ← 3 列
├──────────────────┴──────────────────┼───────────────────────────┤
│      体重 (span-2 / グラフON)       │   (空 or 別ウィジェット) │
├─────────────────────────────────────┴───────────────────────────┤
│      睡眠 (span-2 / グラフON)       │                          │
├─────────────────────────────────────────────────────────────────┤
│              食事記録 (full / グラフON)                         │
├─────────────────────────────────────────────────────────────────┤
│  今日の予定カード (有イベント時のみ)                            │
├─────────────────────────────────────────────────────────────────┤
│  [今週の献立を確認する]                                         │  ← 全幅CTA
└─────────────────────────────────────────────────────────────────┘
```

**Mobile / Tablet S レイアウト:**

```
┌──────────────────────────────────────────┐
│  ダッシュボード        [設定] [同期]     │
├──────────────────────────────────────────┤
│        ← [今日 · 4月30日(水)] →         │
├─────────────┬────────────────────────────┤
│  カロリー   │     PFC                    │  ← 2 列 (Tablet S) / 1列 (Mobile)
├─────────────┼────────────────────────────┤
│  歩数       │  ...                       │
├─────────────┴────────────────────────────┤
│          体重 (full)                     │
├──────────────────────────────────────────┤
│          睡眠 (full)                     │
├──────────────────────────────────────────┤
│          食事記録 (full)                 │
└──────────────────────────────────────────┘
```

### 4.2 日付ナビゲーション

- **前日 `<ChevronLeft>`** / **翌日 `<ChevronRight>`** ボタン
- 中央のラベルをクリックで**今日に戻る**
- 今日の場合: `「今日 · 4月30日(水)」` — ブランドカラー
- 過去/未来: `「4月29日(火)」` — 通常テキスト

### 4.3 ウィジェットグリッド

#### グリッドレイアウト（レスポンシブ）

PC では 3 列、タブレットでは 2 列、モバイルでは 1 列に切り替わる。

| ブレイクポイント | カラム数 | gap |
|---|---|---|
| `< 480px` (Mobile) | 1 列 | `12px` |
| `≥ 480px` (Tablet S) | 2 列 | `12px` |
| `≥ 768px` (Tablet L) | 2 列 | `16px` |
| `≥ 1024px` (Desktop) | **3 列** | `20px` |
| `≥ 1440px` (Wide Desktop) | **3 列** | `24px` |

```css
.widget-grid {
  display: grid;
  grid-template-columns: 1fr;     /* mobile */
  gap: 12px;
}
@media (min-width: 480px)  { .widget-grid { grid-template-columns: repeat(2, 1fr); } }
@media (min-width: 768px)  { .widget-grid { grid-template-columns: repeat(2, 1fr); gap: 16px; } }
@media (min-width: 1024px) { .widget-grid { grid-template-columns: repeat(3, 1fr); gap: 20px; } }
@media (min-width: 1440px) { .widget-grid { gap: 24px; } }

/* span 用ユーティリティ */
.widget-card.span-2 { grid-column: span 2; }   /* 2 col 占有 */
.widget-card.full   { grid-column: 1 / -1; }   /* 全幅 */
```

#### ウィジェットの幅マッピング

| ウィジェット | Mobile (1col) | Tablet (2col) | Desktop (3col) |
|---|---|---|---|
| カロリー (1d) | 1 col | 1 col | 1 col |
| PFC (1d)      | 1 col | 1 col | 1 col |
| 歩数 (1d)     | 1 col | 1 col | 1 col |
| 歩数 (7d/30d) | 1 col | full (2col) | **span-2** |
| 体重 (グラフON) | 1 col | full (2col) | **span-2** |
| 睡眠 (グラフON) | 1 col | full (2col) | **span-2** |
| 食事記録 (グラフON) | 1 col | full (2col) | **full (3col)** |

#### full-width / span-2 になる条件

```js
// グラフ ON 時のみ拡大
if (vis[id].graph) {
  if (id === 'meals')                                       wide = 'full';   // 全列
  else if (id === 'weight' || id === 'sleep')               wide = 'span-2'; // 2列
  else if (id === 'steps' && periods.steps !== '1d')        wide = 'span-2';
}
```

> **設計意図**: 時系列グラフ（体重・睡眠・歩数）は横長の方が読みやすいので 2 列分を確保。リスト形式の食事記録のみ全幅。残りスペースは小ウィジェット（カロリー・PFC等）で詰める。タブレット (2col) では `full` と `span-2` は同じ意味（全幅）になる。

### 4.4 ウィジェットシェル (WidgetShell)

各ウィジェット共通の枠コンポーネント。

```
┌─────────────────────────────────┐
│ 🔖 体重               ⠿        │  ← header (ラベル + ドラッグハンドル)
├─────────────────────────────────┤
│ [数値セクション]                │  数値表示 ON の時のみ
├─────────────────────────────────┤  ← 区切り線（両方 ON 時）
│ 1日 7日 30日                   │  ← PeriodPills（期間切り替えピル）
│ [グラフセクション]              │  グラフ表示 ON の時のみ
└─────────────────────────────────┘
```

- **ドラッグ & ドロップで並び替え**: 300ms長押しで起動
- **並び順**: `localStorage` に永続化

### 4.5 各ウィジェット詳細

#### 体重ウィジェット (`weight`)

- **対応期間**: `7d` / `30d`（1日単位は除外）
- **数値表示**: 最新体重(kg) + 期間内変化(▲▼ Xkg) + 目標達成プログレスバー
- **グラフ**: エリアチャート（Recharts `AreaChart`）
  - X軸: 日付、Y軸: 体重(kg)、グリーングラデーション塗り
  - Y軸ドメイン: `[floor(min-0.5), ceil(max+0.5)]`
- **ボタン**: 「過去の体重を一括同期」

#### カロリーウィジェット (`calories`)

- **対応期間**: `1d` のみ
- **数値表示**: 摂取kcal + 残りkcal / 目標kcal + プログレスバー
  - プログレスバー色: 通常=緑 / 75%超=オレンジ / 100%超=赤
- **グラフ**: ドーナツチャート — 摂取(緑) / 残り(グレー) / 超過(赤)
  - 凡例をドーナツ右横に表示

#### PFCウィジェット (`pfc`)

- **対応期間**: `1d` のみ
- **数値表示**: P/F/C それぞれのグラム数（縦並び、各色付き）
  - P=緑 `#16a34a` / F=オレンジ `#f59e0b` / C=青 `#3b82f6`
- **グラフ**: ドーナツチャート — P/F/C の比率、凡例付き

#### 歩数ウィジェット (`steps`)

- **対応期間**: `1d` / `7d` / `30d`
- **数値表示**: 歩数 + 目標10,000歩 + プログレスバー（100%超=緑）
- **グラフ（1d）**: ドーナツチャート — 達成率%, 歩数表示
- **グラフ（7d/30d）**: 棒グラフ（Recharts `BarChart`）
  - 目標ライン 10,000歩: オレンジ破線
  - `maxBarSize: 18px`, 角丸 `[2,2,0,0]`
- **ボタン**: 「過去の歩数を一括同期」
- **⚠️ 既知の問題**: 日付ナビゲーションを変えてもグラフが変化しない（後述）

#### 睡眠ウィジェット (`sleep`)

- **対応期間**: `7d` / `30d`
- **数値表示**: 睡眠時間(h) + スコア or 目標7時間
- **グラフ**: エリアチャート
  - X軸: 日付、Y軸: 睡眠時間(h)、青グラデーション塗り
- **ボタン**: 「過去の睡眠を一括同期」
- **⚠️ 既知の問題**: 歩数と同様、日付ナビゲーション非連動

> **睡眠と歩数のグラフ独立性**: 睡眠・歩数はそれぞれ独立した期間設定を持ち、一方の期間切り替えが他方のグラフスケールに影響しないよう実装済み。

#### 食事記録ウィジェット (`meals`)

- **対応期間**: `1d` のみ
- **数値表示**: 合計kcal + 食事記録件数
- **グラフ（リスト表示）**: 朝食/昼食/夕食/間食のグループ別リスト
  - 食品名 + kcal の2列表示
- **ボタン群**:
  - 「FatSecret 同期」— 外部サービスから当日の食事日記を取得
  - 「食事記録へ」→ `/meals` ページリンク

### 4.6 未連携状態のウィジェット表示

**設計原則**: データソースに必要な外部サービスが未連携でも、ウィジェットは**常に表示**する。データ部分の代わりに「連携が必要」CTA オーバーレイを表示し、設定ページへの導線を提供する。

#### ウィジェットとデータソースの対応

| ウィジェット | 必要サービス (`required`) | 連携ロジック | 手動入力代替 |
|---|---|---|---|
| 体重 (`weight`) | Fitbit **または** HealthPlanet | OR (どちらか1つでOK) | ✓ 手動追加可能 |
| 歩数 (`steps`) | Fitbit | 必須 | ✗ |
| 睡眠 (`sleep`) | Fitbit | 必須 | ✗ |
| カロリー (`calories`) | （食事ログから自動算出） | — | ✓ 手動or FatSecret |
| PFC (`pfc`) | （食事ログから自動算出） | — | ✓ 手動or FatSecret |
| 食事記録 (`meals`) | （食事ログ自体）| — | ✓ 手動or FatSecret |

> カロリー / PFC / 食事記録は手動入力と FatSecret 同期の両方に対応するため「必須連携」は持たない。ただしデータが0件の時は **FatSecret 連携で自動取得** を促す軽量ヒントを表示。

#### 連携状態によるウィジェット表示の3パターン

##### ① 連携済み + データあり → 通常表示（既存動作）

```
┌─────────────────────┐
│ 🔖 体重         ⠿  │
│ 65.4 kg            │
│ ▼ 0.3 kg           │
│ [プログレスバー]   │
└─────────────────────┘
```

##### ② 必須サービス未連携 → 連携 CTA オーバーレイ

```
┌─────────────────────────┐
│ 🔖 歩数            ⠿    │
├─────────────────────────┤
│   🔌 (LinkOff icon)     │
│                         │
│   Fitbit 連携が必要     │
│                         │
│   [連携設定へ →]        │  ← /me リンク
└─────────────────────────┘
```

- アイコン: `lucide-react/Link2Off`（リンク切れアイコン）
- メッセージ: `${サービス名} 連携が必要`
- CTA: `.btn-primary.btn-sm` で「連携設定へ →」 → `/me`
- ヘッダー（ラベル + ドラッグハンドル）は通常通り表示
- 数値・グラフセクションは描画しない（オーバーレイで置き換え）

##### ③ 任意サービス未連携 + データ0件 → 軽量ヒント

```
┌─────────────────────────┐
│ 🔖 食事記録        ⠿    │
│ —  kcal                 │
│ 0 件の食事記録          │
├─────────────────────────┤
│ 食事記録なし            │
│ FatSecret 連携で        │  ← 軽量ヒント (text-3 サイズ)
│ 自動取得 →              │
│ [食事記録へ] [同期]     │  ← 既存ボタン群はそのまま
└─────────────────────────┘
```

- 既存の空状態 (`EmptyGraph`) を拡張する形で `.btn-ghost` 風のリンク文言を追加
- データが1件でもあれば通常表示に切り替わる

#### 実装ロジック

```js
const WIDGET_REQUIREMENTS = {
  weight: { services: ['fitbit', 'healthplanet'], any: true,  label: 'Fitbit / HealthPlanet' },
  steps:  { services: ['fitbit'],                              label: 'Fitbit' },
  sleep:  { services: ['fitbit'],                              label: 'Fitbit' },
  // calories / pfc / meals は手動入力で機能するため required なし
};

function isConnected(req, connectedServices) {
  if (!req) return true;
  return req.any
    ? req.services.some(s => connectedServices.includes(s))
    : req.services.every(s => connectedServices.includes(s));
}
```

#### スタイル定義（追加するCSS）

```css
.widget-not-connected {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: var(--sp-2);
  padding: var(--sp-5) var(--sp-3);
  text-align: center;
  min-height: 120px;
}
.widget-not-connected-icon {
  color: var(--text-3);
  margin-bottom: 2px;
}
.widget-not-connected-msg {
  font-size: 12px;
  font-weight: 600;
  color: var(--text-2);
  line-height: 1.4;
}
.widget-not-connected-cta {
  font-size: 11px;
  margin-top: 4px;
}
```

### 4.7 ウィジェット設定パネル

ヘッダー右上「設定」ボタンで展開/折り畳み（トグル）。

```
┌──────────────────────────────────────┐
│ ウィジェット表示設定                  │
├──────────────────────────────────────┤
│ 体重         [数値] [グラフ]         │
│ カロリー     [数値] [グラフ]         │
│ PFC          [数値] [グラフ]         │
│ 歩数         [数値] [グラフ]         │
│ 睡眠         [数値] [グラフ]         │
│ 食事記録     [数値] [グラフ]         │
└──────────────────────────────────────┘
```

- 各ウィジェットに「数値」「グラフ」の2つのトグルボタン（アクティブ=緑べた塗り / 非アクティブ=アウトライン）
- 設定・期間選択は `localStorage` に永続化

### 4.8 今日の予定カード

- **表示条件**: Googleカレンダー連携済み**かつ**食事イベント or 運動イベントが1件以上ある場合のみ表示
- 未連携時の代替: ScheduleCard と同じ位置に「Googleカレンダー連携で予定を表示」CTA カード（`.widget-not-connected` と同じスタイル）を表示
- 食事イベント: `Utensils` アイコン
- 運動イベント: `Dumbbell` アイコン
- 時刻順にソート

### 4.9 同期ボタン（ヘッダー右上）

- 選択中の日付のデータをバックエンドから再取得・更新する
- 実行中はアイコンがスピン

---

## 5. マイページ

タブ切り替えUI（`.tab-bar` / `.tab-btn`）で2セクションを切り替え。

```
[設定] [グループ]
```

### 5.1 設定タブ

#### アカウントカード

```
┌────────────────────────────────────┐
│ アカウント                          │
│ ユーザー名                          │
│ メールアドレス                      │
│ [無料プラン (Groq)] または          │
│ [BYOK (anthropic)]                 │
└────────────────────────────────────┘
```

- プラン表示: `connected-badge`（BYOKの場合・緑）/ `disconnected-badge`（無料の場合・グレー）
- ヘッダー右上に「ログアウト」ボタン

#### 健康目標カード

```
目標タイプ: [減量] [維持] [増量]
目標体重 (kg): [___]
1日の目標カロリー (kcal): [___]
達成期限: [1ヶ月] [3ヶ月] [6ヶ月] [1年]  ← プリセットボタン
          [日付picker]  あとXX日
[目標を保存]
```

- 期限プリセット: 今日から N ヶ月後を自動計算してセット
- 保存するとダッシュボードのプログレスバーに反映

#### 外部サービス連携

**連携サービス一覧**:

| サービス | 取得データ |
|---|---|
| Fitbit | 歩数・睡眠・心拍・体重 |
| HealthPlanet (タニタ) | 体重・体脂肪・筋肉量・BMI |
| FatSecret | 食品検索・食事日記同期 |
| Googleカレンダー | 予定取得（外食・運動） |

**状態表示**:
- 未連携: `[連携する]` ボタン（緑・`WifiOff` アイコン付き）
- 連携済み: `[連携済み]` バッジ（緑・`Wifi` アイコン）+ `[解除]` ボタン（赤）

**HealthPlanet 手動コード入力モーダル**:

HealthPlanet は自動リダイレクトが使えないため、「連携する」クリック時に下記フローをとる。

1. HealthPlanet 認証ページを**新タブ**で開く
2. モーダルダイアログを同時表示
3. ユーザーがリダイレクト後の URL またはコードを貼り付け
4. 「連携する」ボタンでトークン交換を実行

```
┌─────────────────────────────────────────┐
│ HealthPlanet 連携コードの入力           │
│                                         │
│ 新しいタブで認証ページが開きました。    │
│ 許可後に表示される URL またはコードを  │
│ 貼り付けてください。                   │
│ [コードまたはリダイレクトURL貼り付け]  │
│ [連携する]  [キャンセル]               │
└─────────────────────────────────────────┘
```

**FatSecret の補足**:
- 食品検索: 連携なしで利用可能
- 食事日記の自動同期: 連携が必要

#### Googleカレンダー同期ボタン

- Googleカレンダー連携済みの場合のみ表示
- クリックでカレンダーリストを同期

#### LLMプランカード

```
┌────────────────────────────────────┐
│ LLMプラン                           │
│ 現在のプラン: 無料プラン (Groq)    │
│ [登録済みAPIキーのリスト]          │
│ [APIキーを追加 (BYOKに切り替え)]  │
└────────────────────────────────────┘
```

**BYOK対応プロバイダー**:

| ラベル | 写真推定対応 |
|---|---|
| Anthropic (Claude) | ✓ |
| OpenAI (GPT-4o) | ✓ |
| Google Gemini | ✓ |
| Groq (Llama 3.1) | ✗ |
| Mistral AI | ✗ |

- 登録済みキーは末尾4文字のみ表示
- 「削除」ボタン（赤）で個別削除可能

#### 食の好みカード

**ダイエットスタイル**（チップ選択）:
`和食中心` / `洋食中心` / `高タンパク` / `低炭水化物` / `糖質制限` / `ベジタリアン` / `ビーガン`

- 選択状態: 緑チップ（`.chip`）
- 未選択: グレー背景

**除外食材カード**:
- 入力欄 + 「追加」ボタン（Enter キーでも追加）
- 追加済み食材: `.chip.removable`（クリックで削除、`×` サフィックス表示）

### 5.2 グループタブ

- グループの作成・招待・参加（招待トークン）・脱退を管理
- 個人グループはユーザー登録時に自動作成

---

## 6. プランページ

週間献立を AI で生成・管理する。

### 主な機能

- **献立生成**: 目標・嗜好・除外食材を元に AI が週間献立を生成
- **献立確定**: 生成結果を確認してから確定
- **スロット編集**: 共有食/個別食の切り替え・外食変更・カロリー手入力
- **メニュー差し替え**: スロット単位で AI に再生成を依頼
- **栄養再計算**: 献立全体の合計栄養を再計算

### スロット編集モーダル

```
┌──────────────────────────────────────┐
│ ✏️ 朝食 を編集              [✕]     │
│                                       │
│ 共有 / 個別: [共有食] [個別食]       │
│ □ 外食に変更                         │
│   (チェック時: 外食kcal入力欄)       │
│                                       │
│ [保存]  [メニューを差し替える]       │
└──────────────────────────────────────┘
```

---

## 7. 買い物リスト

- 確定した献立プランから食材リストを自動生成
- アイテムをタップでチェック（半透明 + 打消し線）
- レシピへのリンク（`.recipe-link`）付き

---

## 8. 食事ログ

手動で食事を記録するページ（`/meals`）。ダッシュボードの食事記録ウィジェットからリンク。

### 主な機能

- **食品検索**: キーワードで食品データベースを検索（連携不要）
- **バーコードスキャン**: カメラ起動でバーコードから食品を特定
- **写真推定**: 食事の写真から AI が栄養を推定（有料 or 月次上限あり）
- **外部サービス同期**: 連携済みサービスの食事日記をインポート
- **過去からコピー**: 過去の食事記録をそのままコピー追加

---

## 9. 認証画面

### ログイン

- Google ログインボタン
- メール/パスワードフォーム

### 新規登録

- 利用規約・プライバシーポリシーへの同意チェックボックス（必須）
- 同意後にアカウント作成

---

## 10. チャットウィジェット

全ページに常駐するフローティング AI アシスタント。

### 構造

```
[FAB ボタン — 右下固定 (緑・50px円)]
    ↓ クリックで展開
┌─────────────────────────────────────┐
│ 🌿 健康アシスタント          [✕]   │  ← ヘッダー（薄緑背景）
├─────────────────────────────────────┤
│                                     │
│  [AIメッセージ bubble]              │  ← グレー背景・左寄せ
│                [ユーザーメッセージ] │  ← 緑背景・右寄せ
│                                     │
├─────────────────────────────────────┤
│ [テキスト入力]              [送信]  │
└─────────────────────────────────────┘
```

- **位置**:
  - Mobile / Tablet S: 画面右下、ボトムナビの上（`bottom: 68px; right: 16px`）
  - Tablet L 以上: ボトムナビが消えるため `bottom: 24px; right: 24px`
  - Desktop / Wide Desktop: コンテンツの右余白に被らないよう `right: max(24px, calc((100vw - 1280px) / 2 + 24px))` を推奨
- **サイズ**:
  - Mobile: `min(100vw - 24px, 400px) × min(60dvh, 520px)`
  - Desktop: `420px × 600px`（固定でも可）
- **タイピングインジケーター**: AI 応答中に3ドットバウンスアニメーション
- **プラン制限**: 無料プランは月次上限あり。上限到達時にアップグレードを促すメッセージを表示

---

## 11. 既知の課題・TODO

### 🔴 高優先度

#### 歩数・睡眠グラフが日付ナビゲーションに追従しない

- **症状**: 日付ナビで過去の日付を選んでも、7d/30d の棒グラフ・エリアチャートが変化しない
- **原因**: 履歴データの取得が「今日から遡るN日間」固定で、選択日付を参照していない
- **影響範囲**: 歩数ウィジェット（7d/30d）、睡眠ウィジェット（7d/30d）

### 🟡 中優先度

#### 体重グラフも日付追従が必要

- 歩数・睡眠と同様、体重履歴グラフも選択日付を反映していない

#### 睡眠グラフに目標ラインがない

- 歩数グラフには目標10,000歩のオレンジ破線があるが、睡眠グラフに目標7hのラインが未実装

### 🟢 その他

- `MealLogPage`（`/meals`）は旧ページとして残存。将来的にはダッシュボードの食事記録ウィジェットに統合予定
- `/body`, `/group`, `/settings` は `/me` へのリダイレクトのみ（旧 URL との後方互換）
- チャットの月次上限到達メッセージは表示されるが、残り使用回数の表示は未実装

---

## 付録: localStorage キー一覧

| キー | 型 | 初期値 | 内容 |
|---|---|---|---|
| `db-order` | `string[]` | `['weight','calories','pfc','steps','sleep','meals']` | ウィジェット表示順 |
| `db-vis` | `Record<id, {value:bool, graph:bool}>` | 全ウィジェット `{value:true, graph:false}` | 数値/グラフの表示設定 |
| `db-periods` | `Record<id, '1d'|'7d'|'30d'>` | 全ウィジェット `'7d'` | 期間選択状態 |

---

## 付録: ウィジェット対応マトリクス

| ウィジェット | 数値 | グラフ種 | 1d | 7d | 30d | full-width |
|---|:---:|:---:|:---:|:---:|:---:|:---:|
| 体重 | ✓ | エリアチャート | — | ✓ | ✓ | グラフON時 |
| カロリー | ✓ | ドーナツ | ✓ | — | — | — |
| PFC | ✓ | ドーナツ | ✓ | — | — | — |
| 歩数 | ✓ | ドーナツ/棒グラフ | ✓ | ✓ | ✓ | 7d/30d かつグラフON時 |
| 睡眠 | ✓ | エリアチャート | — | ✓ | ✓ | グラフON時 |
| 食事記録 | ✓ | リスト | ✓ | — | — | グラフON時 |
